rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Allow read and write access to everyone for now.
    // This is not secure for production apps.
    // You should restrict access based on user authentication.
    match /{allPaths=**} {
      allow read, write;
    }
  }
}
